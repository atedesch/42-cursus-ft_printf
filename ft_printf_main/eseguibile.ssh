#!/bin/bash

# Crea un file temporaneo
cat /dev/null > main.c

# Concatena il contenuto di tutti i file .c nel file temporaneo
for file in *.c; do
  cat $file >> main.c
done

# Compila il file temporaneo
gcc -o main main.c

# Rimuovi il file temporaneo
rm main.c
